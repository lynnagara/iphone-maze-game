'use strict';

var React = require('react-native');

var {
  Text
} = React;

var Maze = React.createClass({
  render: function() {
    return (
      <Text>The maze screen</Text>
    );
  }
});

module.exports = Maze;
